{
  "name": "@protoscan/figma-plugin",
  "version": "0.0.1",
  "private": true,
  "description": "ProtoScan Figma Plugin — detect dead-end screens, orphan screens, and navigation issues in your prototype",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
