{
  "name": "@protoscan/web",
  "version": "0.0.1",
  "private": true,
  "description": "ProtoScan API — vision proxy, payment webhook, key validation",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Vercel builds serverless functions automatically'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "openai": "^4.77.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
